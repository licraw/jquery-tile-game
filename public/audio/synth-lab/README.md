# Synth Lab drum samples

`kick.wav`, `snare.wav`, `hat.wav`, `perc.wav` — 44.1 kHz 16-bit mono WAV.

Provenance: synthesized in-house for SoftArcade with a deterministic offline
render script (sine sweeps and filtered pseudo-random noise; no external
sample content). Public domain / CC0 — no licensing constraints.
